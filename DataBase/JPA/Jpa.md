# 영속성 컨택스트
```
JPA가 엔티티를 관리하는 논리적인 저장 공간
```

## 역할
- 1차 캐시: 한 번 조회한 엔티티를 보관해서 같은 트랜잭션에서 다시 조회할 때 재사용
- 동일성 보장: 같은 ID로 조회한 엔티티는 같은 객체로 관리
- 변경 감지: 엔티티 값을 변경하면 트랜잭션 종료 시 자동으로 UPDATE 실행
- 쓰기 지연: INSERT, UPDATE, DELETE SQL을 모아 두었다가 flush 시점에 실행